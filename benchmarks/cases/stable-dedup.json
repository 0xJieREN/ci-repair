{
  "schema_version": 1,
  "id": "stable-dedup",
  "category": "unit",
  "broken": "def unique(values):\n    return sorted(set(values))\n",
  "correct": "def unique(values):\n    return list(dict.fromkeys(values))\n",
  "overfit": "def unique(values):\n    return [2, 1]\n",
  "public": "from src.app import unique\nassert unique([2, 1, 2]) == [2, 1]\n",
  "oracle": "from src.app import unique\nassert unique([]) == []\nassert unique([3, 2, 1, 3]) == [3, 2, 1]\n",
  "task": "Remove duplicate hashable values while preserving their first appearance order. An empty input returns an empty list."
}
